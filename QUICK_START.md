# 快速启动指南

## 🚀 系统已启动成功！

### 当前运行状态
- ✅ **后端服务**: http://localhost:5001
- ✅ **前端服务**: http://localhost:3001
- ✅ **管理后台**: http://localhost:5001/admin

### 访问系统
1. 打开浏览器访问: **http://localhost:3001**
2. 你会看到主页，有以下功能：
   - 📤 借钥匙
   - 📥 还钥匙
   - 📋 借用记录（新功能）
   - 👥 用户管理
   - 🗝️ 钥匙管理

### 新功能测试

#### 1. 测试借钥匙（带理由）
1. 点击"借钥匙"
2. 填写个人信息
3. 选择钥匙
4. **必须填写借用理由**
5. 提交

#### 2. 查看借用记录
1. 在主页点击"借用记录"
2. 可以筛选：
   - 全部记录
   - 未归还
   - 已归还
3. 每条记录显示：
   - 借用人信息
   - 钥匙信息
   - 借用理由
   - 借用时间
   - 归还时间（如已归还）
   - 借用时长（如未归还）

### 创建测试数据

如果数据库是空的，可以运行：

```bash
python3 test_setup.py
```

这会创建：
- 4个测试用户（学生和老师）
- 5把测试钥匙
- 4条示例借用记录

### 停止服务器

在终端按 `Ctrl+C` 停止服务器

### 下次启动

使用新的启动脚本：

```bash
./start-new.sh
```

或者手动启动：

```bash
# 终端1 - 启动后端
cd backend
PORT=5001 python3 app.py

# 终端2 - 启动前端
cd frontend
npm run dev
```

## 🔧 故障排除

### 端口被占用
如果5000端口被占用（通常是macOS的AirPlay），系统已配置使用5001端口。

### 依赖问题
如果遇到依赖问题：

```bash
# 后端依赖
pip3 install -r backend/requirements.txt

# 前端依赖
cd frontend
npm install
```

### 数据库问题
系统使用SQLite数据库，数据库文件在 `backend/school.db`

如果需要重置数据库，删除该文件后重启后端即可。

## 📝 新功能说明

详细的新功能说明请查看 `NEW_FEATURES.md`

## 🎯 主要改进

1. ✅ **钥匙重复借用记录** - 每次借用都会创建独立记录
2. ✅ **借用时间记录** - 自动记录借用和归还时间
3. ✅ **借用理由必填** - 用户必须填写借用理由

---

**祝使用愉快！** 🎉
